import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.team.dashboard',
  appName: 'Dashboard EC',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
